## Current Codebase Status

**Backend**: Healthy, rebuilt with indentation fix. Running on port 8001.

**Config**: JWT algorithm now RS256 in `config.yaml` ✓

**Tests**: `pytest.ini` and `test_config.yaml` added — secret now set for test env ✓

**Database**:
- Migration `2add_user_ownership` created — adds `user_id` to `containers` ✓
- Models updated: `Container.user_id`, `User.containers` relationship ✓
- Ownership helper `check_container_ownership()` implemented ✓

**API Endpoints — Ownership Enforcement**:
- Updated: containers (list, get, update, metrics, logs, env, ports, favorite), metrics_history, alerts, schedules (list/create/update/delete), alert_rules (list/create/update/delete), bulk operations ✓
- Direct queries replaced with `check_container_ownership()` where needed
- Token payload now includes `user_id` for all auth tokens ✓

**Compilation**: All backend modules compile cleanly ✓

**Frontend**: Running (healthcheck flaky, but app serves)

---

## Remaining High-Priority Tasks

| Task | Status |
|------|--------|
| Apply DB migration (`alembic upgrade head`) | **PENDING** — migration file created but not executed inside container |
| Verify ownership checks on all container action endpoints (start/stop/pause/unpause — already use direct query but admin-only; fine but could be simplified) | DONE |
| Update bulk operation endpoints to pass `current_user` | DONE |
| Redis integration for token blacklist persistence | TODO |
| Add `DOCKWATCH_ENCRYPTION_KEY` env & use for TOTP secret encryption | TODO |
| Encrypt backup archives | TODO |
| Enforce container resource limits (CPU/memory) on create | TODO |
| Persist settings to DB (instead of config file) | TODO |
| Update `docs/API.md` with new endpoints | TODO |
| Remove duplicate `frontend/src/hooks/useTheme.tsx` | TODO |
| Add session idle timeout | TODO |
| Add 2FA backup codes | TODO |
| Add Prometheus metrics endpoint | TODO |
| Fix nginx healthcheck (optional) | TODO |

**Plan file created**: `.kilo/plans/1777198331166-neon-lagoon.md`

Want me to continue with the remaining implementation?

## Todo List

- [x] Fix config.yaml JWT algorithm mismatch (HS256 -> RS256)
- [x] Add DOCKWATCH_JWT_SECRET to pytest.ini for test env
- [~] Add user_id FK to containers table, create migration, update ownership logic
- [ ] Integrate Redis for token blacklist persistence
- [ ] Add DOCKWATCH_ENCRYPTION_KEY for TOTP secret encryption
- [ ] Encrypt backup archives
- [ ] Enforce container resource limits (CPU/memory) on create
- [ ] Persist settings to DB instead of config file
- [ ] Update docs/API.md with new endpoints
- [ ] Remove duplicate useTheme.tsx, keep ThemeContext.tsx
- [ ] Fix frontend healthcheck (optional)
- [ ] Add session idle timeout
- [ ] Add 2FA backup codes
- [ ] Add Prometheus metrics endpoint
- [ ] Run tests to verify fixes
- [ ] Rebuild and restart containers